const getters = {
  goodsCategoryFormState: state => state.goodsCategory.formState,
  goodsCategoryList: state => state.goodsCategory.categoryList,
  templateConfigList: state => state.contentTemplate.templateList,
}
export default getters